# AI Skills

Reusable skills for Codex and Claude Code.

## Install

Run:

```bash
bash install.sh
```

This copies each skill into both global skill directories:

- `~/.codex/skills/`
- `~/.claude/skills/`

The installed copies are separate from this source repository, so accidental edits by one tool do not affect the other tool or the Git source.
